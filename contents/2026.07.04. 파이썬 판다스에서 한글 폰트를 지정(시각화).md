다음 코드를 쓰면 된다.

```python
plt.rc('font', family='Malgun Gothic')
```

혹은

```python
plt.rc('font', family='Gulim')
```

그리고, 데이터 파일을 분석할 때에는...
그 데이터 파일의 값에 콤마나 겉은 정수인데 실제 타입은 문자형인 것들이 있다.
이러한 케이스를 주의할 것.

엑셀 앱을 열어 직접 휴먼의 눈으로 검수하고 조치하는 것이 코드를 통한 분석 이전에
선행되어야 한다.
